CMAKE_MINIMUM_REQUIRED(VERSION 3.4)
PROJECT(t1)
MESSAGE("SOURCE PROJECT DIR IS: "${PROJECT_SOURCE_DIR})
MESSAGE("BINARY PROJECT DIR IS: "${PROJECT_BINARY_DIR})
SET (SRC_LISTS main.cpp)
ADD_EXECUTABLE(demo ${SRC_LISTS})